{
  "name": "luminare-visitor-counter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "db:init": "wrangler d1 execute luminare_visitor_counter --file=./schema.sql --remote"
  },
  "devDependencies": {
    "wrangler": "^4.18.0"
  }
}

